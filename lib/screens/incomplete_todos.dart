import 'package:flutter/material.dart';

class IncompleteTodos extends StatelessWidget {
  const IncompleteTodos({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Incomplete Todos"));
  }
}
